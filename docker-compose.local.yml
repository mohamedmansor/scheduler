volumes:
  webtask_scheduler_local_postgres_data: {}
  webtask_scheduler_local_postgres_data_backups: {}
  webtask_scheduler_local_redis_data: {}


services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: webtask_scheduler_local_django
    container_name: webtask_scheduler_local_django

    depends_on:
      - postgres
      - redis
    volumes:
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/local/postgres/Dockerfile
    image: webtask_scheduler_local_postgres
    container_name: webtask_scheduler_local_postgres
    volumes:
      - webtask_scheduler_local_postgres_data:/var/lib/postgresql/data
      - webtask_scheduler_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  redis:
    image: docker.io/redis:6
    container_name: webtask_scheduler_local_redis
    
    volumes:
      - webtask_scheduler_local_redis_data:/data
    

  celeryworker:
    <<: *django
    image: webtask_scheduler_local_celeryworker
    container_name: webtask_scheduler_local_celeryworker
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: webtask_scheduler_local_celerybeat
    container_name: webtask_scheduler_local_celerybeat
    restart: always
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: webtask_scheduler_local_flower
    container_name: webtask_scheduler_local_flower
    ports:
      - '5555:5555'
    command: /start-flower
